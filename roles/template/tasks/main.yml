---
# ./roles/template/tasks/main.yml

- raw: apt-get -y install python3 python3-apt
  register: raw_command

- debug:
    var: raw_command

- name: install dependencies
  apt:
    name: "{{ item }}"
    state: present
  become: true
  loop:
    - sudo
